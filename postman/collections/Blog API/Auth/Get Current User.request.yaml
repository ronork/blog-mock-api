$kind: http-request
name: Get Current User
method: GET
url: '{{baseUrl}}/auth/me'
order: 5000
description: |-
  Returns the profile of the currently authenticated user (password excluded).

  **Requires:** Bearer token authentication.
