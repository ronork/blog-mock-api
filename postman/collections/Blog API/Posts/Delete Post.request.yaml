$kind: http-request
name: Delete Post
method: DELETE
url: '{{baseUrl}}/posts/{{postId}}'
order: 5000
description: |-
  Delete a post by ID. Only the post's author can delete it. Returns `204 No Content` on success.

  **Requires:** Bearer token authentication (must be the post author).
